# Tệp chứa khóa API và thông tin xác thực (KHÔNG BAO GIỜ đưa lên git)
config/api_keys.json
credentials.yml
*.env
.env

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── Môi trường ảo (Virtual Environment) ─────────────────────
venv/
.venv/
env/
.env/
ENV/

# ── Cấu hình IDE ─────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── Output của Rasa (tạo ra lúc train/test, không phải source) ──
.rasa/
models/*.tar.gz
results/
train_test_split/

# ── File tạm của workflow (generated at runtime) ─────────────
New_questions.txt
unlableled_intents.json
responses.json
